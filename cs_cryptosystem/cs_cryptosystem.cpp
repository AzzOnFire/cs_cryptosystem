#include "stdafx.h"

int main() {
    std::cout << "Hello World!\n";

    // string text ("some text");
    // IV<512>iv_1(text);
    // byte* iv = iv_1.init_vector();
    // byte* key = generate_key<512>();
    // auto derived_key = create_derived_key<512>(key,iv);
}
